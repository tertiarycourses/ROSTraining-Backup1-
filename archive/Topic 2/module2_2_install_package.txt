# Type sudo apt install ros-melodic-calibration-msgs.
sudo apt install ros-melodic-calibration-msgs

# Type sudo apt remove ros-melodic-calibration-msgs to remove the package.
sudo apt remove ros-melodic-calibration-msgs

# Clone the fake_ar_publisher repository into the catkin workspace’s src directory.
# Use Ctrl-Shift-V to paste within the terminal, or use your mouse to right-click and select paste
cd ~/catkin_ws/src

# Build the new package. The build command can be issued from anywhere inside the catkin workspace
catkin build

source ~/catkin_ws/devel/setup.bash

# Run rospack find fake_ar_publisher to verify the new packages are visible to RO
rospack find fake_ar_publisher
git clone https://github.com/jmeyer1292/fake_ar_publisher.git